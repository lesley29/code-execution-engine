ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
}

connectionStrings {
    mongodb = "mongodb://user:password@localhost:27017"
}